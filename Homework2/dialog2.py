## An Introduction to Tkinter# dialog2.py## Copyright (c) 1997 by Fredrik Lundh## fredrik@pythonware.com# http://www.pythonware.com#from Tkinter import *import tkMessageBoximport tkSimpleDialogclass MyDialog(tkSimpleDialog.Dialog):    def body(self, master):        Label(master, text="Session name:").grid(row=0)        Label(master, text="Max players (min 2):").grid(row=1)        self.e1 = Entry(master)        self.e2 = Entry(master)        self.title('Session creation')        self.e1.grid(row=0, column=1)        self.e2.grid(row=1, column=1)    def validate(self):        first = self.e1.get().strip()        second = self.e2.get().strip()        if not all(c.isalnum() for c in first) or len(first) < 2:            tkMessageBox.showwarning("Bad input", "Bad or too short game name")            return 0        try:            second = int(second)            if second < 2:                tkMessageBox.showwarning("Bad input", "Bad number of max players")                return 0        except:            tkMessageBox.showwarning("Bad input", "Bad number of max players")            return 0         self.result = first, second        return 1    def apply(self):        pass